apiVersion: apps/v1
kind: Deployment
metadata:
  name: hypercrawler-frontier-service
spec:
  template:
    spec:
      containers:
        - name: hypercrawler-frontier-service
          resources:
            requests:
              memory: 756Mi
              cpu: "0.1"
            limits:
              memory: 756Mi
              cpu: "2"
